describe("[APP] Este es un ejemplo de prueba unitaria", () => {
    test("Esto deberia retornar...", () => {
  
      const a = 10
      const b = 8
      const c = a + b
      expect(c).toEqual(18)
  
    });
  });